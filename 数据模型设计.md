# PM Risk Copilot：数据模型设计 v1.1

## 1. 数据模型设计目标

本文服务于面向芯片项目 PM 的全流程风险副驾驶（PM Risk Copilot）。供应链延期是 MVP 首个验证场景，数据模型同时保留未来扩展研发、测试、质量和生产风险的能力。

模型用于支持以下闭环：

```text
Event
  ↓
Risk Agent
  ↓
Rule / Case Knowledge Base（RAG）/ Impact Analysis
  ↓
Risk
  ↓
Risk Card 供 PM 确认
```

Event 表示项目过程中产生的风险相关事实，Risk 表示 Risk Agent 分析后的结果。Rule 和 Case Knowledge Base（RAG）不是独立 Agent，Risk Agent 是分析编排中心。

## 2. 整体数据流

```text
项目相关信息
    ↓
Event 标准化
    ↓
Risk Agent 启动风险分析
    ↓
调用：
  - Rule 规则能力
  - Case Knowledge Base（RAG）
  - Impact Analysis 能力
    ↓
Risk Agent 综合判断
    ↓
生成 Risk 结果
    ↓
生成 Risk Card 供 PM 确认
```

项目相关信息可以包括项目状态变化、异常事件、团队反馈和风险相关文档信息。MVP 阶段优先使用模拟项目数据验证完整链路。

## 3. 核心实体模型

### 3.1 Project

项目基础信息。

核心字段：

- `project_id`
- `project_name`
- `product`
- `stage`
- `owner`
- `target_date`

### 3.2 Milestone

项目关键节点，用于关联项目进展和风险影响。

核心字段：

- `milestone_id`
- `project_id`
- `name`
- `planned_date`
- `status`
- `delay_days`

### 3.3 Event

Event 代表项目过程中产生的风险相关事件，只描述客观事实，不代表最终风险，也不包含 AI 风险判断结果。

核心字段：

- `event_id`
- `project_id`
- `milestone_id`
- `event_type`
- `source`
- `supplier`
- `material`
- `material_category`
- `delay_days`
- `description`
- `occurred_at`
- `extra_data`

字段说明：

- `milestone_id`：该事件主要影响的项目节点。
- `extra_data`：不同事件类型的扩展属性，例如产能下降或材料短缺信息。

示例：

```json
{
  "event_id": "EV001",
  "project_id": "NV685",
  "milestone_id": "M004",
  "event_type": "supplier_delay",
  "material": "BGA基板",
  "delay_days": 15,
  "extra_data": {}
}
```

### 3.4 Rule

Rule 用于提供确定性风险判断依据。

核心字段：

- `rule_id`
- `rule_name`
- `risk_domain`
- `threshold`
- `risk_level`
- `suggest_action`

MVP 阶段：

- `threshold` 以字符串形式保存规则描述，例如 `delay_days>=14`。
- 不实现通用规则解析器或复杂规则 DSL。
- Rule 由 Risk Agent 按固定约定解释，用于辅助风险等级和建议判断。

### 3.5 Case

Case 是历史经验案例，用于 Case Knowledge Base（RAG）检索和 Risk Agent 知识增强，不设计独立的 Case Agent。

核心字段：

- `case_id`
- `title`
- `category`
- `domain`
- `context`
- `problem`
- `cause`
- `impact`
- `solution`
- `result`
- `risk_pattern`
- `trigger_conditions`
- `recommendation`
- `tags`

Case Knowledge Base（RAG）用于辅助 Risk Agent 进行风险判断、影响分析和建议生成。

### 3.6 Risk Output v1.0

Risk 不是原始事实数据，而是 Risk Agent 综合 Event、Rule、Case Knowledge Base（RAG）和 Impact Analysis 后生成的风险结果。Risk 用于风险卡片展示、风险历史记录和 PM 审核闭环。

每条 Risk 必须使用以下唯一字段定义：

#### 来源关联

- `risk_id`
- `event_id`
- `project_id`
- `milestone_id`

#### 风险判断

- `risk_type`
- `risk_level`

`risk_level` 使用受控值：`Low`、`Medium`、`High`、`Critical`。

#### 风险内容

- `risk_title`
- `risk_summary`
- `risk_reason`

#### 检索结果

- `matched_rules`
- `matched_cases`
- `case_match_reason`

其中 `matched_rules` 和 `matched_cases` 为数组。

#### 影响与建议

- `impact_analysis`
- `recommendations`

其中 `recommendations` 为数组。

#### 人工协作

- `risk_status`
- `review_status`
- `reviewer`
- `review_comment`

`risk_status` 可使用：`open`、`processing`、`resolved`、`closed`。

`review_status` 可使用：`pending`、`confirmed`、`rejected`。`reviewer` 和 `review_comment` 在 MVP 阶段可以为空。

#### 时间

- `created_at`

Risk Output v1.0 示例结构与 `data/output/risks.json` 保持一致：

```json
[
  {
    "risk_id": "RISK001",
    "event_id": "EV001",
    "project_id": "NV685",
    "milestone_id": "M004",
    "risk_type": "supplier_delay",
    "risk_level": "High",
    "risk_title": "BGA基板延期影响封测完成节点",
    "risk_summary": "BGA基板供应商延期15天，可能导致封测完成节点延后。",
    "risk_reason": "命中延期规则并匹配相似历史案例。",
    "matched_rules": ["R001"],
    "matched_cases": ["CASE001"],
    "case_match_reason": "事件与历史案例均涉及BGA基板延期和封测节点影响。",
    "impact_analysis": "可能影响封测完成节点和客户验证计划。",
    "recommendations": ["启动第二供应商验证"],
    "risk_status": "open",
    "review_status": "pending",
    "reviewer": "",
    "review_comment": "",
    "created_at": "2026-08-04T09:00:00+08:00"
  }
]
```

### 3.7 Action

Action 不属于 MVP 核心数据模型，调整为未来扩展能力。

MVP 阶段，Collaboration Agent 主要负责：

- 风险通知
- PM 确认
- 简单协同

未来可扩展：

- 任务创建
- SLA 跟踪
- Action Item 管理

## 4. 数据存储策略

MVP 阶段，数据默认使用飞书 Aily 内部能力管理，包括：

- 项目数据
- Event 事件数据
- Case 知识库
- Rule 规则库
- Risk 结果记录

Mock JSON 文件仅用于：

- Demo 初始化
- 数据准备
- Agent 测试

当前 Mock 文件与数据对象对应关系：

```text
data/mock/projects.json   → Project 初始化数据
data/mock/milestones.json → Milestone 初始化数据
data/mock/events.json     → Event 初始化数据
data/mock/rules.json      → Rule 初始化数据
data/mock/cases.json      → Case 知识库初始化数据
data/output/risks.json    → Risk 输出示例
```

未来如需接入企业系统，可通过数据接口进行数据适配和扩展；这不改变 MVP 默认使用飞书 Aily 内部能力的设计。

## 5. MVP 数据流示例

```text
项目相关信息中出现：BGA供应商延期15天
              ↓
Event 标准化为 EV001
              ↓
Risk Agent 启动分析
              ↓
调用 R001 规则
              ↓
Case Knowledge Base（RAG）匹配 CASE001
              ↓
Impact Analysis 识别 M004 封测完成节点影响
              ↓
生成 RISK001
              ↓
生成 Risk Card
              ↓
PM 确认
```

## 6. 后续扩展

未来可扩展：

- 研发风险
- 测试风险
- 质量风险
- 生产风险
- 更多风险领域的 Rule 和 Case
- 企业系统数据接口
- Collaboration Agent 的任务创建、SLA 跟踪和 Action Item 管理

扩展应保持 Risk Agent 作为分析编排中心，不将 Rule、Case Knowledge Base（RAG）或 Impact Analysis 设计为独立 Agent。
