# Demo 供应链风险 Agent：数据模型设计 v1.1

> 本文用于 14 天 Demo 的数据模型、Mock 数据和 Agent 协作约定。当前 Demo 聚焦“BGA 基板延期 15 天”这一核心场景，支持后续扩展其他供应链风险，但不要求在 Demo 阶段实现企业级规则引擎、向量库管理或完整数据库治理能力。

本模型服务于面向芯片项目 PM 的全流程风险副驾驶（PM Risk Copilot）。供应链延期是 MVP 首个落地场景，数据结构同时为未来的研发、测试、质量和生产风险保留扩展能力。

## 1. 设计目标与范围

Demo 主流程如下：

```text
Event 客观事件
    ↓
Risk Agent
    ├── 查询 Rule 规则库
    ├── 查询 Case 历史案例库（RAG）
    ↓
Risk 风险判断结果
    ↓
Action 行动任务（可选）
```

当前重点是 `supplier_delay / BGA 基板延期`。模型保留 `material_category`、`event_type`、`risk_type` 等扩展点，可支持后续的材料短缺、产能不足和质量异常等场景。

## 2. 数据模型设计

### 2.1 对象与关系

| 对象 | 说明 | 关系 |
|---|---|---|
| Project | 芯片研发项目 | 一个项目包含多个 Milestone、Event |
| Milestone | 项目关键节点 | 归属于一个 Project，用于判断影响 |
| Event | 客观发生的业务事件 | 归属于一个 Project，可产生一个或多个 Risk |
| Rule | 简单、可读的确定性判断依据 | 被 Risk Agent 查询 |
| Case | Synthetic Industry Cases（行业模拟脱敏案例） | 被 Risk Agent 用于 RAG 检索 |
| Risk | Risk Agent 分析后的风险判断结果 | 关联一个 Event，可引用多个 Rule/Case |
| Action | 风险处置任务 | 归属于一个 Risk，可选 |

### 2.2 核心字段设计

以下字段为 Demo MVP 建议字段。未列出的业务字段可按现有需求保留，但不应阻塞 Demo 开发。

| 对象 | Demo MVP 字段 |
|---|---|
| Project | `project_id`, `project_name`, `product`, `stage`, `owner`, `target_date` |
| Milestone | `milestone_id`, `project_id`, `name`, `planned_date`, `status`, `delay_days` |
| Event | `event_id`, `project_id`, `milestone_id`, `risk_domain`, `source_department`, `event_type`, `source`, `supplier`, `material`, `material_category`, `delay_days`, `description`, `occurred_at`, `extra_data` |
| Rule | `rule_id`, `rule_name`, `risk_domain`, `threshold`, `risk_level`, `suggest_action` |
| Case | `case_id`, `title`, `category`, `domain`, `context`, `problem`, `cause`, `impact`, `solution`, `result`, `risk_pattern`, `trigger_conditions`, `recommendation`, `tags` |
| Risk | `risk_id`, `event_id`, `project_id`, `risk_type`, `level`, `reason`, `evidence`, `impact`, `recommendation`, `rule_ids`, `case_ids` |
| Action | `action_id`, `risk_id`, `owner`, `task`, `deadline`, `status` |

通用约定：使用可读字符串 ID（如 `NV685`、`EV001`）；日期使用 `YYYY-MM-DD`，时间使用 ISO 8601；`rule_ids[]` 和 `case_ids[]` 在 Demo 阶段可直接保存为数组。

Event 字段说明：

- `milestone_id`：表示该事件主要影响的项目节点。
- `risk_domain`：风险所属领域，例如 `supply_chain`、`engineering`、`testing`、`quality`。
- `source_department`：风险来源部门或角色，例如 `supplier`、`R&D`、`test_team`。
- `extra_data`：用于存储不同事件类型的扩展属性，避免 Event 为每类事件无限增加固定字段。

例如，供应商产能下降和材料短缺事件可以分别使用：

```json
{
  "capacity_reduction": 20
}
```

```json
{
  "material_inventory_shortage": true
}
```

### 2.3 Event 与 Risk 职责说明

Event 表示客观发生的业务事件，不包含 AI 判断结果，因此删除 `Event.risk_type`。Event 只描述“发生了什么”，例如：

```json
{
  "event_type": "supplier_delay",
  "material": "BGA基板",
  "delay_days": 15
}
```

这表示“供应商发生延期事件”，而不是“供应链风险”。

Risk 表示 Risk Agent 分析后的风险判断结果，保留 `risk_type`。例如：

```json
{
  "risk_type": "supply_chain",
  "level": "High"
}
```

这表示 AI 判断该事件属于供应链风险。

Risk Agent 不限定供应链领域。当前 MVP 优先处理 `supplier_delay`；未来可支持研发延期、测试失败和质量异常等项目风险事件。

## 3. Agent 数据流设计

### 3.1 Risk Agent 输入

Risk Agent 接收的输入数据包括 `Project`、`Milestone`、`Event`。核心示例：

```json
{
  "project_id": "NV685",
  "event_type": "supplier_delay",
  "material": "BGA基板",
  "delay_days": 15
}
```

### 3.2 Knowledge 数据

Risk Agent 从以下知识数据中检索：

- `Rule`：提供确定性判断依据，例如 `delay_days>=14` 对应 High 风险。
- `Case`：提供历史经验参考，用于 RAG 检索和解释建议。

MVP 阶段，Rule 的 `threshold` 字段采用字符串形式保存规则描述，例如：

```json
"milestone.delay_days>=7"
```

当前 Demo 不实现通用规则解析引擎。规则由后端固定逻辑或 Risk Agent 根据约定进行解释；未来产品化后，可进一步升级为结构化规则引擎。

### 3.3 Risk Agent 输出

输出为 `Risk`，至少包含：

- `risk_level`（在数据模型中对应 `level`）
- `reason`
- `evidence`
- `impact`
- `recommendation`

如需要形成执行闭环，再根据 Risk 生成可选的 `Action`。

### 3.4 Risk Agent Output 模型

Risk 不是原始事实数据，而是 Risk Agent 对 `Event`、`Rule` 和 `Case` 进行分析后的结果。Risk 用于：

- 风险卡片展示
- 风险历史记录
- PM 人工审核闭环

Risk Output v1.0 使用以下字段：

| 字段 | 说明 |
|---|---|
| `risk_id` | 风险唯一 ID |
| `event_id`、`project_id`、`milestone_id` | 风险来源事件、项目和受影响节点 |
| `risk_type` | 与事件类型兼容的风险类型 |
| `risk_level` | 规则判断得到的风险等级，仅使用 `Low`、`Medium`、`High`、`Critical` |
| `risk_title`、`risk_summary`、`risk_reason` | 风险卡片标题、摘要和 AI 判断原因 |
| `matched_rules`、`matched_cases` | 命中的规则 ID 和历史案例 ID 数组 |
| `case_match_reason` | 历史案例与当前风险相似的原因 |
| `impact_analysis` | 风险可能造成的影响 |
| `recommendations` | Risk Agent 生成的建议数组 |
| `risk_status` | 风险状态：`open`、`processing`、`resolved`、`closed` |
| `review_status` | 审核状态：`pending`、`confirmed`、`rejected` |
| `reviewer`、`review_comment` | 人工审核人和审核意见，MVP 可为空 |
| `created_at` | 风险生成时间 |

主 Demo 的输出链路为：

```text
EV001 事件
   ↓
M004 受影响节点
   ↓
R001 命中规则
   ↓
CASE001 相似案例
   ↓
RISK001 风险输出
```

`data/output/risks.json` 是 Risk Agent 输出数据的示例文件，根节点保持数组格式，便于后续作为风险结果记录导入 Supabase 的 `risks` 数据表。

## 4. JSON Schema

以下说明描述 Demo Mock 数据的最小结构。每个 JSON 文件对应一个数据表初始化文件，文件本身采用数组格式，即文件根节点为 `[{...}]`，每个对象代表一条待初始化的数据记录。实现方可增加非核心字段，但不得把 Event 的 AI 判断结果写入 Event。

```json
[
  {}
]
```

Mock 文件与 Supabase 数据表的对应关系如下：

| Mock 文件 | 对应数据表 | 说明 |
|---|---|---|
| `projects.json` | `projects` table | Project 初始化数据 |
| `milestones.json` | `milestones` table | Milestone 初始化数据 |
| `events.json` | `events` table | Event 初始化数据 |
| `rules.json` | `rules` table | Rule 初始化数据 |
| `cases.json` | `cases` table | Case/RAG 初始化数据 |
| `risks.json` | `risks` table | Risk 结果数据 |
| `actions.json` | `actions` table | Action 任务数据 |

该组织方式便于后续将各 JSON 文件分别导入 Supabase PostgreSQL 对应数据表。

对象字段示例：

```json
{
  "event": {
    "event_id": "EV001",
    "project_id": "NV685",
    "milestone_id": "M004",
    "event_type": "supplier_delay",
    "source": "模拟数据",
    "supplier": "供应商A",
    "material": "BGA基板",
    "material_category": "BGA",
    "delay_days": 15,
    "description": "供应商预计延期15天",
    "occurred_at": "2026-08-03T09:30:00+08:00",
    "extra_data": {}
  },
  "rule": {
    "rule_id": "R001",
    "rule_name": "BGA延期高风险规则",
    "threshold": "delay_days>=14",
    "risk_level": "High",
    "suggest_action": "启动备选供应商验证"
  },
  "risk": {
    "risk_id": "RISK001",
    "event_id": "EV001",
    "project_id": "NV685",
    "risk_type": "supply_chain",
    "level": "High",
    "reason": "BGA基板延期15天，超过14天阈值。",
    "evidence": ["命中规则 R001", "关联案例 CASE001"],
    "impact": "封测节点预计延后。",
    "recommendation": "启动备选供应商验证。",
    "rule_ids": ["R001"],
    "case_ids": ["CASE001"]
  }
}
```

## 5. Mock 数据规范

### 5.1 Demo Mock 数据规模要求

- `Project`：1-3 个项目。
- `Milestone`：每个项目 3-5 个关键节点。
- `Event`：3-5 个供应链事件。
- `Rule`：3-5 条规则。
- `Case`：至少 10 个历史案例，其中 BGA 延期相关 5 个，其他供应链风险 5 个。
- `Risk`：根据 Event 自动生成。
- `Action`：按需生成，不作为核心必选能力。

核心演示事件固定为：BGA 基板延期 15 天，并由规则和案例共同支持 High 风险判断。

### 5.2 Case 数据说明

Case 不是企业真实数据，而是 `Synthetic Industry Cases`（行业模拟脱敏案例），仅用于 RAG 检索和 Demo 展示。每个案例必须形成完整闭环，至少包含：

`problem` 问题、`cause` 原因、`impact` 影响、`solution` 解决方案、`result` 结果、`tags` 标签。

## 6. Database 建议

当前目标是支持 Demo 运行，不是完成完整企业级数据库设计。

建议保留 `projects`、`milestones`、`events`、`rules`、`cases`、`risks`、`actions` 等基础表的设计方向。`risk_rules`、`risk_cases` 关联表暂不强制实现；Demo 阶段允许在 `Risk` 中使用 `rule_ids[]`、`case_ids[]` 数组保存引用。

规则仅保留简单的 `threshold` 文本，不引导开发完整规则 DSL 或通用规则执行引擎。案例在 Demo 阶段可以使用普通文本/关键词检索模拟 RAG。

## 7. 数据存储策略

目的：明确区分 Demo Mock 数据、业务数据库和 Agent 数据流，避免将系统误解为只能运行静态 Mock 数据的 Demo。

### 1. 数据存储分层

系统分为三个层：

#### 数据初始化层

Mock 数据用于：

- 初始化 Demo 环境
- Agent 测试
- 联调阶段使用

形式为 JSON 或 CSV 文件，例如：

```text
mock/
├── projects.json
├── milestones.json
├── events.json
├── rules.json
└── cases.json
```

Mock 数据不是系统运行时的唯一数据来源。Demo 运行后，业务数据应通过后端 API 或飞书输入动态写入数据库。

#### 业务数据层

建议使用 Supabase PostgreSQL 作为 Demo 的主要数据存储，保存以下业务数据：

`projects`、`milestones`、`events`、`rules`、`cases`、`risks`、`actions`

数据库保存系统运行过程中的业务状态，包括动态新增的事件、Agent 生成的风险结果和可选的行动任务。

#### 展示与交互层

飞书作为：

- 用户输入入口
- 风险展示入口
- 协同入口

飞书多维表格或相关页面不作为核心业务数据库，核心业务状态以 Supabase PostgreSQL 为准。

### 2. 数据流说明

```text
Mock 初始化数据 / 飞书输入事件
            ↓
       Backend API
            ↓
    Supabase 数据库
            ↓
      Risk Agent 读取
  Project / Milestone / Event
       Rule / Case
            ↓
        生成 Risk
            ↓
      Risk 写回数据库
            ↓
     前端 / 飞书展示结果
```

其中，Risk Agent 从数据库读取 `Project`、`Milestone`、`Event`、`Rule` 和 `Case`，完成分析后生成 `Risk`，再将 Risk 写回数据库供前端和飞书展示。

### 3. MVP 阶段实现原则

14 天 Demo 不要求复杂数据治理。优先保证：

1. 数据可以动态新增。
2. Agent 可以读取数据库数据。
3. Risk 结果可以保存。
4. 前端可以展示实时结果。

不要实现：

- 完整企业数据仓库
- 复杂权限系统
- 数据治理平台

### 4. 数据库选型说明

推荐使用 Supabase PostgreSQL，原因是：

- 快速部署
- 提供完整的数据库能力
- 方便 Web 开发
- 支持后续 RAG 扩展

飞书多维表格定位为协作展示层，不作为核心数据库。这样可以保留飞书的输入、展示和协同体验，同时确保业务状态、Risk 结果和 Agent 数据流有统一的数据存储位置。

## 8. Future Extension

以下字段保留为未来扩展字段，不属于 14 天 Demo MVP 的实现要求：

| 归属 | 未来扩展字段 | 用途 |
|---|---|---|
| Case | `embedding`, `embedding_model`, `indexed_at` | 向量生成、模型记录和索引管理 |
| Risk Agent 运行记录 | `model_name`, `prompt_version` | 模型与提示词版本追踪 |

未来可进一步增加规则结构化执行、向量数据库、关联表、审计字段、置信度评估和更多供应链风险类型，但这些能力不应成为当前 Demo 的前置依赖。

## 9. Demo 验收标准

- Event 不包含 `risk_type`，Risk 保留 `risk_type`。
- `EV001` 能关联项目 `NV685` 及受影响 Milestone。
- BGA 基板延期 15 天能命中 `threshold: delay_days>=14`，生成 High 风险。
- Risk 能追溯命中的 Rule 和 Case。
- Mock 数据规模满足第 5 章要求，且 Case 均为行业模拟脱敏案例。
- 后端、飞书和 Agent 开发可依据本文件完成 MVP 协作；未来扩展不阻塞当前演示。
